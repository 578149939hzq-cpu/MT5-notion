## 1. 配置与数据模型

- [x] 1.1 新增 `accounts.json` 读取与校验逻辑，提供账户配置数据结构和默认路径解析。
- [x] 1.2 为交易对象和 Notion 字段映射新增 `account_name / 所属账户`，并将重复判断升级为“账户 + ticket”组合键。

## 2. 多账户轮询与增量同步

- [x] 2.1 拆分 MT5 终端初始化、联网检查、账户登录和关闭逻辑，支持单次初始化下的多账户轮询。
- [x] 2.2 实现按账户查询 Notion 最新同步时间的增量窗口计算，并保留默认时间窗与安全回看缓冲。
- [x] 2.3 重构主流程，按账户顺序执行登录、交易提取、指标计算、Notion 写入、错误隔离和账户切换延迟。

## 3. 验证与交付

- [x] 3.1 扩展自动化测试，覆盖账户配置加载、账户维度去重、增量窗口和轮询异常处理。
- [x] 3.2 更新项目文档与示例配置，补充 `accounts.json` 使用方式、Notion 新字段要求，并确保忽略敏感文件。
