## 1. 仓库敏感信息净化

- [x] 1.1 将 `accounts.example.json` 改回纯示例文件，移除真实账户、服务器与密码，只保留占位符或假数据结构。
- [x] 1.2 审查并清理 `archive/` 目录中仍保留的硬编码生产标识或误导性运行配置，必要时补充“历史参考、非当前入口”的说明。

## 2. 运行输出收紧

- [x] 2.1 为主同步脚本设计统一的最小暴露日志策略，移除或掩码完整 MT5 登录号、余额、服务器名、终端数据路径与 Notion 标识输出。
- [x] 2.2 更新诊断工具的终端输出，只报告配置状态与连通性结果，不回显 token 前缀、完整数据库 ID 或其他敏感值。

## 3. 文档与回归保护

- [x] 3.1 更新 README，明确 `.env` / `accounts.json` 属于本地私有配置，`*.example.*` 文件只能是示例；同时提醒历史日志可能包含旧敏感信息，不应直接分享。
- [x] 3.2 补充回归测试，覆盖关键脱敏逻辑与示例文件卫生约束，防止敏感输出或真实示例数据回退。
