# Archived Scripts

These files are historical references only.

- They are not the current supported entrypoint.
- Use `mt5_notion_sync.py` in the repository root for active sync work.
- Do not treat any file under `archive/` as a source of live credentials or production configuration.
